export type FormData = {
  name: string;
  email: string;
  company: string;
  projectType: string;
  budget: string;
  message: string;
};


export type LocationState = {
    from?: {
        pathname?: string;
    };
};